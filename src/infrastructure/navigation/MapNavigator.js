import React from "react"
import { createStackNavigator, TransitionPresets } from "@react-navigation/stack"

const MapStack = createStackNavigator()

const MapNavigator = () => {
  return <div>MapNavigator</div>
}

export default MapNavigator
